# タスク6-1 — アプリUI用CSS完成

**作業日**: 2026-03-27
**対象ファイル**: `index.html`

---

## 実装内容

### 追加・修正内容

| 対象 | 変更内容 |
|------|---------|
| `body` | `line-height: 1.6` を追加 |
| `.card h2` | `border-bottom: 2px solid #e6f5ec` でセクション見出しを強調 |
| `.step-panel` | `fadeIn` アニメーション（0.2s, translateY 6px→0）でステップ切り替えを滑らか化 |
| レスポンシブ | `@media (max-width: 640px)` でグリッド・フレックス列を折り返しに変更 |

### レスポンシブ対応の詳細

- `.field-grid`: 2カラム → 1カラム
- `.proj-row1` / `.proj-row2`: フレックスラップ有効化
- `.member-row1` / `.member-row2`: フレックスラップ有効化
- `.trainee-row`: フレックスラップ有効化
- `.card`: パディング・マージン縮小

---

## 完了確認

- [x] `body` に `line-height` が設定されている
- [x] カード見出しにボーダーラインがある
- [x] ステップ切り替えでフェードインアニメーションが動作する
- [x] 640px 以下でフォームが縦積みになる

---

## 次タスクへの引き継ぎ

- **タスク6-2**: CLAUDE.md の機能チェックリストに沿って動作検証を実施
