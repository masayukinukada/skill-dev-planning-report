# タスク1-2 — 大枠ブロック配置

**作業日**: 2026-03-27
**対象ファイル**: `index.html`

---

## 実装内容

- `.app-header`（ヘッダー）: グリーン背景、タイトル・サブテキスト
- `.step-nav`（ステップナビ）: 4タブ、`goStep(n)` を `onclick` で呼び出し
- `#step0`〜`#step3`（ステップパネル）: step0のみ表示、step1〜3は `hidden` 属性
- `#preview-wrap`（プレビューエリア）: `hidden` 属性で非表示
- 状態変数 `currentStep / projIds / memberIds / reportHTML` を宣言
- `goStep(n)` 関数を実装（タブ切り替えの基本動作）
- 各パネルに開発用プレースホルダーを配置（後続タスクで順次置換）

---

## 設計上の決定事項

| 決定内容 | 選択理由 |
|---------|---------|
| ステップIDを `step0`〜`step3`（0始まり） | JS の配列インデックスと一致させてシンプルに管理 |
| 表示切替は `hidden` 属性で制御 | CSS `display:none` との二重管理を避ける |
| `goStep()` をタスク1-2で先行実装 | タブクリックの動作確認をこのタスク内でできるようにする |
| ヘッダー・ナビCSSを暫定で追加 | 後続タスク（2-1/2-2）で詳細化するが、構造確認のために最低限のスタイルを付与 |
| `.step-btn.done` のアクセントカラーをオレンジ（`#F39346`）に | テーマカラー方針に従い、完了済み状態をオレンジで表現 |

---

## 完了確認

- [x] `#step0` のみ表示、`#step1〜3` と `#preview-wrap` は非表示
- [x] 4つのステップタブが横並びで表示される
- [x] タブクリックでステップパネルが切り替わる
- [x] `「1基本情報」` タブが `active`（グリーン）で表示される
- [x] コンソールエラーなし

---

## 次タスクへの引き継ぎ

- **タスク2-1**: `.app-header` のCSSを詳細化（現在は暫定スタイル）
- **タスク2-2**: `.step-nav` / `.step-btn` のCSSを詳細化（`step-num` スタイル等）
- **タスク3-1〜3-5**: 各 `#step*` パネルのプレースホルダーを実際のフォームに置換
- プレースホルダー（`.placeholder-note`）は全フォーム実装後に削除
